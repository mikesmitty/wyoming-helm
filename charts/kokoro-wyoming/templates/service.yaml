apiVersion: v1
kind: Service
metadata:
  name: {{ include "kokoro-wyoming.fullname" . }}
  labels:
    {{- include "kokoro-wyoming.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: wyoming
    protocol: TCP
    name: wyoming
  selector:
    {{- include "kokoro-wyoming.selectorLabels" . | nindent 4 }}
